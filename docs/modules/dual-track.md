# 轨道扩容（原双轨模块）

当前规则已替换为 **1/2/3 条双向共享车道**，不再区分固定方向车道。资源、调度、视觉偏移及旧存档兼容见 [shared-lanes.md](shared-lanes.md)。

升级为批量原子预检：所有 ID、重复选择、既有待办和总预算一起验证，费用为 `10 × 单元长度 × 新增车道数`。空闲单元立即升级；繁忙单元托管费用、阻止新计划进入，等待旧行程与安全净空结束后执行。取消待办全额退回托管费用，拆除退款包含实际支付的升级款。

停车与平台周转见 [scheduler.md](scheduler.md)，平台不再作为新服务计划的最终停车位。
