# 验证记录

## MVP v0.0.2 发布前检查（2026-09-12）

- 沿用历史提交 `mvp v0.0.1` 的版本序列；package.json、锁文件及更新记录统一为 0.0.2。
- GitHub Pages 专用构建（含类型检查）通过，编辑辅助与候选/时钟的 7 项定向检查通过，差异格式检查通过。按用户偏好未重跑完整套件或长时间浏览器试玩。

## 城市概览增长开关（2026-09-12）

- 将城市增长控制从设置移到检查器「城市概览 → 清湾」标题下，改为整行可点的滑动开关；文字同步显示持续增长或已暂停，仍发送原有 growth 命令。
- 类型检查、生产构建及差异格式检查通过；本轮未做浏览器试玩，未更改增长规则。

## 逐分钟时钟与时间流速（2026-09-12）

- 顶栏恢复逐分钟显示；基础流速由 20 调至 30 模拟秒/实秒（提高 50%），1× 下每 2 实秒推进 1 模拟分钟，保留 2×/4×。模拟与渲染使用同一 TIME_SCALE，物理速度与预约步长不变。
- 时钟/候选预览与连续运动的 12 项针对性检查通过，覆盖分钟切换、跨日、1×/2×/4× 插值、暂停及重置；类型检查与生产构建通过。未进行浏览器试玩。

## 统一图上建造菜单（2026-09-12）

- 空地、建筑、站点、停车位与轨道统一提供「铺轨 / 停车位 / 站点」三个大按钮。站点/停车位通过鼠标选择方向并预览，点击提交，Esc 返回；删除原方向下拉框。
- 查看模式可点选设施、查看名单与追踪，保留平移缩放；不显示建造/拆除/升级/改入口/购售 Pod 操作。详情跳转不自动切到编辑，切模式保留所选对象。查看模式推迟到真实拖动后才捕获指针，避免吞掉点击。
- 4 项编辑辅助逻辑检查通过：既有轨道最近接点、泊位外侧起点、空地鼠标方向与无扣费预览、贴楼平台/独立停车区别。覆盖预览与模拟命令落点一致、占用位置及预算不足时不可建造。
- 类型检查、生产构建和差异格式检查通过。按用户偏好，未进行本轮浏览器试玩或完整测试套件，未发布 GitHub Pages。

## 全局空格暂停（2026-09-12）

- 空格在捕获阶段统一暂停/继续，不激活当前按钮；按下重复与释放不重复切换，输入控件与介绍弹窗除外。地图对象仅保留 Enter 激活。
- 新增模拟侧 `toggle-pause`，不依赖旧 UI 快照翻转状态。类型检查与生产构建通过；按用户要求未继续运行玩法或浏览器测试。

## 查看/编辑模式与独立停车（2026-09-12）

- 停车位无建筑/站点归属，支持指定空地点与接轨方向；站旁快速添加仅为选址便利，不保留所有权。更新了对应回归用例，但本轮按用户要求未运行玩法测试或完整测试套件。
- Tab 切换查看/编辑；查看仅平移缩放，编辑点空地选择站点、停车位或轨道。地图操作按钮按屏幕尺度固定为 36px 高。
- 类型检查、生产构建、差异格式检查通过；未进行本轮浏览器试玩，也未发布 GitHub Pages。下方为历史版本记录，不作为本轮全部验收结论。

## 经营与生活 v2（2026-09-12）

- 类型检查与生产构建通过。`economy-v2`、`travel-choice-v2`、`grants`、`economy-budget`、`city`、`city-life-v2`、`persistence`、`integration` 共 35 项针对性检查通过。
- 覆盖里程票价/调价、3 次手动补助、单笔贷款/还款、资产维护与实际空驶/载客成本、借款不计收入、现金与分类累计对账、账目截断、v2 存储校验和在途恢复。居民直接到本楼平台候车、下车即进楼；车费只计算载客路程，现有行程不被中途调价改变。
- 学校/医护/餐饮/公园及时间需求、昼间重新选择活动、有限目的地分配与新建筑生成有定向检查；不做旧版存档迁移，不删除旧文件。
- Chrome 本地页面已检查票价编辑生效、三次补助耗尽后禁用、贷款入账不增加经营利润、成本规则和分类累计可见。观察到首轮增长出现学校/餐厅与真实居民；楼内居民检查器正确区分下一目的地与上次选择，并显示没有平台所以步行。控制台未报告错误或警告。
- 不将以下历史版本验证当作 v2 全量验收；本轮没有长时间人类试玩、满载性能测试或更新 GitHub Pages。

## 可复现检查

在仓库根目录运行：

```bash
npm run typecheck
npm run build
npm test
```

这些命令覆盖类型、生产构建和 Vitest 单元/集成/对抗测试。`npm run test:browser` 当前没有作者测试，不应作为已通过的浏览器结果。

上一轮构建（包含手动补助、居民追踪、共享车道与前后段缓冲）通过；针对 `grants`、`focus`、`shared-lanes` 的 12 项回归通过。安全缓冲子任务另外完成相关 34 项定向检查。此前共享车道阶段曾通过 93 项全套检查，但这些不是持续城市增长改动后的全套结果。

## 持续增长改动

- 本轮生产构建与类型检查通过；`city`、`city-growth`、`development-demand`、`grants`、`persistence`、`network`、`population` 共 38 项定向检查通过。
- 覆盖连续 10 轮超过旧 14 栋限制、地图扩大但旧位置不变、布局尺寸差异、基础设施与有效步行路径避让、有限岗位入住、暂停不补刷、旧完成存档恢复发展、中心权重与个体错峰、累计补助和扩图保存。
- 独立 `localhost` 页简检：住宅显示 24/48 与下一批住户时间；旧存档办公楼显示真实任职人数、已开放岗位、完整岗位容量与就业中心说明；地图阶段/中心标记可见，控制台无警告或错误。
- 按用户要求没有重新进行长时间试玩或 120 栋/1,200 人/160 Pod 满载验证，不把容量保护上限当作已保证的帧率或吞吐。

## 已验证边界

- 轨道/节点/对角交叉/泊位资源互斥、有限停车尾部、实体守恒、保存恢复、经济账本和增长逻辑。
- 生产容量比较：8 个同时需求在基线与扩容网络中的 Pod/步行分流（见 `docs/modules/capacity.md`）。
- 曾验证旧 14 栋、156 人、30 个已连接 Pod 的 3,600 模拟秒负荷夹具。它不代表持续增长版本的 120 栋/1,200 人/160 Pod 满载结果。
- 独立浏览器验收已观察到 UI 建设、可见乘客标记、连续移动、暂停保存/读取和增长至 14 栋/156 名居民。
- 浏览器已验证点击整段选中 23 小段，Command 追加到 28 小段、一次升级 28 小段（预算 6,391 → 6,111），Shift 跨路口连选扩展到 36 小段。
- 浏览器已验证选中高亮连续且无逐格圆点叠加；聚焦选中轨道时未再出现反馈截图中的巨大蓝圈。
- 建筑检查器已验证当前意图、真实已完成步行/Pod 分流、住宅 24 名居民的工作分布与可展开姓名名单；地图数量与面板一致。
- 批量拆除在独立 `localhost` 测试城市验收：7 段折线 + 4 段独立轨道；点击、Command 多选、V/X 切换保留选区、Alt 单格后 Shift 绕角连选均通过。确认前仍有 20 段轨道；一次确认后只剩原始 9 段，选区清空，预算 1,768 → 1,900，准确退回 132。未拆动玩家正在运行的 `127.0.0.1` 城市进行此项测试。
- 同一独立城市又验证检查器批量拆除 3 段，准确退款 36；对会切断周转泊位的两段拆除请求，界面保留两段选区并显示原因，轨道和预算均未改变。浏览器未报告错误或警告。

## 待补或明确不声称

本轮按用户要求不继续详细浏览器测试。已做的独立浏览器检查包括：铺轨候选合法为绿、穿楼为红，鼠标预览不扣钱或落轨；引导关闭后刷新仍保持关闭。居民室内/室外追踪与手动补助做必要逻辑回归，新增共享车道及安全缓冲做针对性检查。新规则下的长时间试玩留给用户，不把先前浏览器结果冒充本轮全面验收。

满负载城市的浏览器帧率、任意网络的长期交互仍待验收；导出下载需确认浏览器实际落盘。尚未证明任意网络无死锁、30 分钟人类可玩性、挑战失败流程、附近建筑平台接驳、编组或 3D。增长按时间推进，不能将其描述为稳定性门槛。
